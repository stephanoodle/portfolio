title: Stephanie Coulter
description: Support engineer working on documentation and AI — building with AI agents, writing docs AI can use, and testing whether the answers are right.
lang: en

# Project site at stephanoodle.github.io/portfolio. Set to "" if it moves to its own domain.
baseurl: "/portfolio"

# GitHub Pages turns links to .md files into links to the rendered pages.
plugins:
  - jekyll-relative-links
relative_links:
  enabled: true
  collections: false

exclude:
  - README.md
